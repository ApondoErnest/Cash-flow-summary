# Restore drill environment (Step 117) — generated by restore-drill.sh from a backup snapshot.
# Do not edit manually unless you know what you are doing.
# Never commit deploy/env/restore-drill.env

# Created from: production.env.snapshot inside a backup run directory.
# APP_KEY must match the restored database (copied from snapshot).

RESTORE_DRILL_HTTP_PORT=8082
HTTP_PORT=8082
APP_URL=http://127.0.0.1:8082
