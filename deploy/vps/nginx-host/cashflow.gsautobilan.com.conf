# Host reverse proxy — cashflow.gsautobilan.com
# Install:
#   sudo cp deploy/vps/nginx-host/cashflow.gsautobilan.com.conf /etc/nginx/sites-available/cashflow-summary.conf
#   sudo ln -sf /etc/nginx/sites-available/cashflow-summary.conf /etc/nginx/sites-enabled/
#   sudo nginx -t && sudo systemctl reload nginx
# TLS (adds/modifies the listen 443 block — keep proxy_set_header lines below):
#   sudo certbot --nginx -d cashflow.gsautobilan.com
#
# After certbot, confirm BOTH :80 and :443 location blocks include:
#   proxy_set_header X-Forwarded-Proto $scheme;

server {
    listen 80;
    listen [::]:80;
    server_name cashflow.gsautobilan.com;

    client_max_body_size 64M;

    location / {
        proxy_pass http://127.0.0.1:8081;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 600s;
    }
}

# Optional explicit HTTPS block (certbot may merge this — paths must match your cert):
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name cashflow.gsautobilan.com;
#
#     ssl_certificate /etc/letsencrypt/live/cashflow.gsautobilan.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/cashflow.gsautobilan.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     client_max_body_size 64M;
#
#     location / {
#         proxy_pass http://127.0.0.1:8081;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 600s;
#     }
# }
