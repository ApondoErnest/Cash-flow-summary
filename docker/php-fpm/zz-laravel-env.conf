; PHP-FPM clears container environment in workers by default (clear_env=yes).
; Compose env_file injects APP_KEY and other Laravel secrets into the container.
; Without this, CLI (artisan/diagnose) works but HTTP via PHP-FPM returns 500.
[www]
clear_env = no
